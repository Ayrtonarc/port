// Hero component removed — kept as a stub to avoid breaking imports while
// components directory is being optimized. Remove this file if you are sure
// nothing imports `Hero` anymore.
export default function Hero() {
  return null;
}
